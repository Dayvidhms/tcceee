<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.felipe.tsttcc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastroActivity" />
        <activity
            android:name=".SobreNosActivity"
            android:label="@string/title_activity_sobre_nos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OperacoesActivity"
            android:label="@string/title_activity_operacoes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddItem_Activity"
            android:label="@string/title_activity_add_item_"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>